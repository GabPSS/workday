import 'package:flutter/material.dart';

class TaskEditorWidget extends StatelessWidget {
  const TaskEditorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
